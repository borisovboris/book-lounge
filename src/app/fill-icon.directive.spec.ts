import { FillIconDirective } from './fill-icon.directive';

describe('FillIconDirective', () => {
  it('should create an instance', () => {
    // const directive = new FillIconDirective();
    // expect(directive).toBeTruthy();
  });
});
